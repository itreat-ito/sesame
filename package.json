{
  "name": "Sesame!",
  "version": "1.0.0",
  "private": true,
  "description": "Sesame! | Bypass BASIC Auth",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "generate-icons": "node scripts/generate-icons.mjs",
    "package": "npm run build && cd dist && zip -r ../sesame-extension.zip . && cd .."
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.4.0",
    "sass": "^1.98.0",
    "vite": "^8.0.3"
  },
  "dependencies": {
    "micromodal": "^0.6.1",
    "sortablejs": "^1.15.7",
    "toastify-js": "^1.12.0"
  }
}
